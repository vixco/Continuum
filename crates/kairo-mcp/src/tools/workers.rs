//! # Worker tools (`mcp__kairo__workers`)
//!
//! - `spawn_worker` — spawn a new Claude Code worker session
//! - `worker_status` — get current status of a worker
//! - `worker_cancel` — stop a worker
