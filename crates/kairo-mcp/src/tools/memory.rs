//! # Memory tools (`mcp__kairo__memory`)
//!
//! - `memory_query` — semantic search over episodic memory
//! - `memory_write` — store a new memory entry
//! - `memory_fact_get` — read a semantic fact
//! - `memory_fact_set` — write a semantic fact
//! - `memory_recent` — get raw log for last N minutes
