// Temporary stub. Task 12 rewrites this tab against the new memory vault
// API (see apps/desktop/src/lib/tauri.ts memory* wrappers + memoryTheme.ts
// + memoryViews.ts). The previous raw/episodic/semantic UI called Tauri
// commands that no longer exist on the backend (Task 10 removed them).
export function MemoryTab() {
  return null;
}
