name: Legacy release workflow (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Use Publish release
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "This legacy workflow is retired."
          echo "Cross-platform releases are built and published by .github/workflows/publish.yml after CI succeeds on main."
